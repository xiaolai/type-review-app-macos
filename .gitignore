# SwiftPM build products and the assembled bundle. Both are reproducible from
# source with `make`, and both are binaries — tracking them made every commit
# carry thousands of object files.
.build/
TYPE.app/
TypeReview.app/
.DS_Store

# Per-developer Claude Code config, including which plugins are enabled here.
# Ignored in the web repo too; it is a choice about tooling, not about the app.
.claude/
